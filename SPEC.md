# インターバルタイマー（watchOS）実装仕様

野球の練習で使う、Apple Watch 単体で動くインターバルタイマー。
全体時間と分割回数を入れると、**全体の残り時間と現在の区切りの残り時間を同時に表示**する。

- 想定端末: Apple Watch Series 11
- 構成: watchOS 単体アプリ（iPhone コンパニオンなし）
- 言語/UI: Swift / SwiftUI
- 参考: 挙動確認用の HTML プロトタイプが別途あり。数値の見え方と色替えのタイミングはそれに準拠する

---

## 1. 機能仕様

### 1.1 設定画面（起動時）

| 項目 | 範囲 | 初期値 |
|---|---|---|
| 全体時間（分） | 1〜180 | 20 |
| 分割回数 | 1〜12 | 4 |

- 入力は Digital Crown で増減。グローブ装着時でもタップできるよう、操作要素は最小44pt相当を確保する
- 「1区切り ◯分◯秒」をリアルタイムでプレビュー表示する
- 直近の設定は `@AppStorage` で保存し、次回起動時の初期値にする
- プリセットを3枠まで保存できる（名前＋全体時間＋分割回数）。初期プリセットは入れなくてよい
- 「開始」ボタンで実行画面へ遷移

### 1.2 実行画面

表示要素（上から）:

1. 全体の残り時間（小さめ、`m:ss`）
2. **現在の区切りの残り時間（画面の主役、`m:ss`）**
3. 「3 / 4」のような区切り位置
4. 区切り数ぶんのセグメントバー（消化済み／進行中／未消化を塗り分け）
5. 一時停止 / リセット

例: 全体20分・分割4で開始した直後は `20:00` と `5:00`。1秒後は `19:59` と `4:59`。

### 1.3 区切りの通知

| タイミング | 表示 | ハプティクス |
|---|---|---|
| 区切りの残りが20%以下になった瞬間 | 背景色を警告色に変える | 軽い1発（`.click`） |
| 区切りが0になった瞬間 | 背景色を通常色に戻す／次の区切りへ | 強い（`.notification` を2回、間隔0.15秒） |
| 全体が0になった瞬間 | 終了画面 | 最も強い（`.notification` を3回＋`.stop`） |

- 20%通知は各区切りにつき1回だけ発火する（フラグは区切りが変わったらリセット）
- 区切りの長さ×0.2 が2秒未満になる場合は、20%通知を発火させない（短すぎて意味がないため）
- 屋外で視認するので、警告色は明度差で判別できる配色にする（色相の差だけに頼らない）

### 1.4 状態

`設定 → 実行中 ⇄ 一時停止 → 終了` 。終了画面からは「もう一度」で同設定で再スタート、「設定に戻る」の2択。

---

## 2. タイマーロジック（ここが最重要）

**`Timer` で1秒ずつ減算する実装にしないこと。** 画面消灯・スリープ・アプリ非アクティブでズレる。

- 開始時刻を `Date` で保持し、経過時間は常に「現在時刻 − 開始時刻 ＋ 一時停止までの累積」で算出する
- 区切りの境界は `全体秒数 × i / 分割数`（i = 1...分割数）で都度計算する。1区切りの秒数を足し込んでいくと丸め誤差が蓄積するため
- 表示は切り上げ（`ceil`）。開始直後に `20:00` と出て、`19:59` に落ちるようにする
- 画面の秒表示更新には `Text(timerInterval:pauseTime:countsDown:)` を使う。システム側が描画を維持するので、常時表示（Always-On）状態でもアプリのコードを動かさずに数字が進む。ただし**区切り判定とハプティクスは自前のタイマーで行う**必要があるため、両者を併用する
- 自前の判定ループは0.1秒間隔で十分（1秒間隔だと20%到達の検出が最大1秒遅れる）

---

## 3. バックグラウンド動作

練習中は腕を下ろすし、他アプリも触る。アプリが停止するとタイマーとして使い物にならないので、以下を実装する。

**方針: `HKWorkoutSession` を使う。**

- `HKWorkoutConfiguration` の `activityType = .baseball`、`locationType = .outdoor`
- セッションが `.running` の間、アプリはフォアグラウンド相当の扱いになり、実行が継続される。実際に運動している場面なので用途としても正当
- 必要な設定:
  - HealthKit capability を有効化
  - Info.plist に `NSHealthShareUsageDescription` / `NSHealthUpdateUsageDescription`
  - Background Modes に Workout processing
- タイマー開始でセッション開始、終了/リセットでセッション終了。ワークアウトは保存する（保存しないと目的外利用と見なされるリスクがある）
- 権限が拒否された場合は `WKExtendedRuntimeSession` にフォールバックする。こちらは連続動作時間に上限があるため、その旨を設定画面に1行表示する

---

## 4. ファイル構成（提案）

```
IntervalTimer/
  IntervalTimerApp.swift
  Models/
    TimerConfig.swift      // 全体秒数・分割数・プリセット
    TimerEngine.swift      // 経過時間の算出、区切り判定、状態遷移。UIに依存させない
  Services/
    WorkoutKeeper.swift    // HKWorkoutSession のラッパー
    Haptics.swift          // 3種類の通知パターン
  Views/
    SetupView.swift
    RunView.swift
    DoneView.swift
    Components/SegmentBar.swift
```

`TimerEngine` は `Date` を注入できる形にして、テストから時刻を任意に進められるようにする。

---

## 5. 受け入れ基準

1. 全体20分・分割4で開始した直後に `20:00` と `5:00` が表示される
2. 1秒後に `19:59` と `4:59` になる
3. 各区切りの残り1分（20%）で背景色が変わり、軽い振動が1回だけ起きる
4. 5分・10分・15分の各時点で強い振動が起き、区切り表示が `2 / 4` `3 / 4` `4 / 4` と進む
5. 20分時点で終了画面になり、最も強い振動が起きる
6. **開始後に30秒アプリを閉じて戻っても、残り時間がズレていない**
7. **腕を下ろして画面が消え、30秒後に腕を上げても残り時間がズレていない**
8. 一時停止中は数字が止まり、再開すると停止していた時間ぶんは経過していない
9. 全体7分・分割3のように割り切れない設定でも、最後の区切りが0で終わり、端数が残らない
10. 分割1（＝分割なし）でも動作する
11. 全体1分・分割12（1区切り5秒）のとき、20%通知が発火しない

---

## 6. v1のスコープ外

以下は作らない。後から追加する。

- iPhone アプリ／iPhone との同期
- コンプリケーション、ウィジェット
- 音声・読み上げ
- 区切りごとに異なる長さを設定する機能（例: 30秒運動 / 10秒休憩の繰り返し）
- 練習記録の保存・履歴

---

## 7. 実装時に確認したい点

- 屋外・直射日光下での視認性。輝度を上げても判別できる配色になっているか実機で確認する
- HKWorkoutSession 稼働中のバッテリー消費。60分の練習で許容範囲か
